# Backend location used by the React frontend.

# When running the frontend directly with npm:
# REACT_APP_API_BASE=http://localhost:8000

# The Docker Compose HTTPS build overrides this value with:
# REACT_APP_API_BASE=/api
#
# Nginx then proxies /api/* requests to the backend container.